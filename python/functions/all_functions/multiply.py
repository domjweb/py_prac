# standard multiplication function, works in most circumstances
def multiply(a, b):
    return a * b

# lambda function that produces the same effect as an anonymous function
multiply = lambda x, y: x * y



